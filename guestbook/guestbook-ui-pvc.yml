apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: home-rw-pvc
  namespace: default
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 1Gi
  volumeName: pearc26-impasta-rw-pv
  storageClassName: ""
